---

# This is a placeholder vars file for healthchat-workspace role
# Role-specific computed variables can be defined here if needed

# Auto-generate docker-compose content from template
healthchat_workspace_docker_compose_file_content: |
  {{ lookup('template', role_path + '/templates/docker-compose.yml.j2') | indent(width=2, first=False) }}